// Adding a new property

let demo =  {
        name : 'Siva',
        age  :  25 ,
};
console.log(demo);

demo.city = 'Tirunelveli'
console.log(demo);

let car = {
    Brand : 'TOYATO',
    Year  : 2005,
    Model : 'cerry',
    DetailsInfo : function (){
            return ` This Car Brand is ${this.Brand} and Year is ${this.Year} and Model is ${this.Model}`
    },
    
};
console.log(car.DetailsInfo());


let shop ={
}

// Modifying an existing property

// Object with method

// Destructuring assignment

//Nested Complex Objects

// Accessing properties of the nested objects

// Destructure owner object

// Output details about the restaurant

// Output the menu items using destructuring within forEach
